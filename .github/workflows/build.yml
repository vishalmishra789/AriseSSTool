name: Build Arise SS EXE
on: [push]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build JAR with Maven
        # This creates the AriseSSTool-1.0.0.jar in the target folder
        run: mvn clean package

      - name: Debug - List Files in Target
        run: dir target/

      - name: Create EXE with jpackage
        # This bundles the JAR and the Java Runtime into a single EXE installer
        run: |
          jpackage --type exe `
          --dest . `
          --name AriseSSTool `
          --input target/ `
          --main-jar AriseSSTool-1.0.0.jar `
          --main-class com.arise.ss.Launcher `
          --win-shortcut `
          --win-menu `
          --vendor "AriseDev" `
          --app-version "1.0.0"

      - name: Debug - List Root Directory
        run: dir

      - name: Upload EXE Artifact
        uses: actions/upload-artifact@v4
        with:
          name: AriseSSTool-Windows-Installer
          path: "*.exe"
          retention-days: 5
